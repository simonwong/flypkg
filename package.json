{
  "name": "flypkg",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "ci:publish": "pnpm install --frozen-lockfile false && echo \"build\" && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.18.1"
  }
}
