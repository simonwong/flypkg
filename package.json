{
  "name": "flypkg",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "ci:publish": "echo \"build\" && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.18.1"
  }
}
